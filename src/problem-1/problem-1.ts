/*
  - modify the function to check if an input is a palindromic string or not
  - return true if the input is a palindrome
  - return false if the input is not a palindrome
*/

const isPalindrome = (input) => {
  // write your code here
};

export default isPalindrome;
